# TODO

- Download modal: the progress bars should always be visible — pin them
  outside the scrollable viewport rather than letting them scroll away.
- Call `hf` with the `--json` argument and rebuild the output parser to
  consume its structured JSON instead of scraping human-readable output.
